import { Boundary } from "@/components/boundary";
import Form from "./form";

export default function Page() {
  return (
    <Boundary
      labels={["intercepted-replaced-routes/form/page.tsx"]}
      size="small"
      color="violet"
    >
      <Form />
    </Boundary>
  );
}
